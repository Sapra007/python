#Count occurrences of an element in a tuple
tuple = (1, 2, 3, 4, 5, 1, 2, 1, 3, 1)
element_to_count = 1
count = tuple.count(element_to_count)
print(f"Element {element_to_count} occurs {count} times in the tuple.")