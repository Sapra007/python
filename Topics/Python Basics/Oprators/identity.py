x = [1, 2, 3]
y = [1, 2, 3]

print(x == y)
print(x is y)

#This code demonstrates identity operators in Python:
#Equal to (==) checks if the values are the same,   
#while 'is' checks if both variables point to the same object in memory.
